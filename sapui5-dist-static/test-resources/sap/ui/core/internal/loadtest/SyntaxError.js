// this is a module with a syntax error
jQuery.sap.declare("sap.ui.core.test.SampleModule");

// check for a 'this' where it isn't allowed
//function some() {
//	this.this.toString();
//};

// trailing comma in array 
var values = ["1", "2",];


var otherValues = { "1" : "1", "2": "2", };

window.myFunction(sParam) {
  // do something	
};


